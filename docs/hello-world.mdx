---
id: hello-world
slug: /hello-world
title: Сәлем, Әлем!
sidebar_label: Сәлем, Әлем!
---

import ToggleCodeBlock from '@site/src/components/CodePythonBlock';

**«Сәлем, Әлем!»** бағдарламасы — жаңа бағдарламалау тілін үйреніп жатқан жаңадан бастаушылар жазатын ең бірінші бағдарлама. Бұл жай ғана консольге "Hello, World!" деген жазуды шығаратын қарапайым бағдарлама.

### Тапсырма

Пайдаланушыдан есімін сұрайтын және "Сәлем, xx!" деген сәлемдесу хабарламасын шығаратын бағдарлама жазыңыз.

#### JavaScript-те жүзеге асыру

<ToggleCodeBlock
  title="📄 hello.js"
  language="javascript"
  code={`const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question('Атыңызды енгізіңіз: ', (answer) => {
  console.log(\`Сәлем, \${answer}!\`);
  rl.close();
});`}
/>

Жоғарыдағы кодты терминалда node hello.js пәрмені арқылы іске қосуға болады.

#### Python-дағы жүзеге асыру

<ToggleCodeBlock
  title="📄 hello.py"
  language="python"
  code={`name = input("Атыңызды енгізіңіз: ")
print(f"Сәлем, {name}!")`}
/>

Жоғарыдағы кодты терминалда python hello.py пәрмені арқылы іске қосыңыз.


### Код ерекшеліктері

- Python тілінде пайдаланушы енгізуін алу үшін input() функциясы қолданылады, ал JavaScript тілінде readline API-лері пайдаланылады.
- Python-да мәтін қосу үшін f"..." синтаксисі пайдаланылады, ал JavaScript-те — backtick (`) таңбалары.
- Python-да экранға шығару үшін print() функциясы, ал JavaScript-те console.log() қолданылады.

### Жылдам салыстыру

| Feature | JavaScript | Python |
|---------|------------|--------|
| Файл кеңейтімі | `.js` | `.py` |
| Іске қосу командасы	 | `node filename.js` | `python filename.py` |
| Экранға шығару | `console.log()` | `print()` |
| Түсініктеме	 | `// comment` or `/** comment */` | `# comment` or `""" comment """` |

### Ресурстар

- https://docs.python.org/3/library/functions.html#print
