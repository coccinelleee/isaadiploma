---
id: os-module
slug: /os-module
title: OS модулі
sidebar_label: OS модулі
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';


Python тіліндегі `os` модулі файлдар мен бумаларды басқаруға және жүйеге байланысты ақпараттарға қол жеткізуге арналған функцияларды ұсынады.

### Тапсырма

Берілген буманың ішіндегі барлық файлдар мен бумаларды тізіп шығатын бағдарлама жазыңыз. Әр элементтің атауын басып шығарып, ол файл ма, әлде бума ма екенін көрсетіңіз.

#### JavaScript іске асыруы
<CodePythonBlock
  title="📂 Буманың ішін тізімдеу (Python)"
  language="python"
  code={`import os

def list_items(directory):
    items = os.listdir(directory)

    for item in items:
        item_path = os.path.join(directory, item)
        if os.path.isdir(item_path):
            print(f"{item}: бума")
        else:
            print(f"{item}: файл")
list_items('../')`}
/>

#### Python іске асыруы

<CodePythonBlock
  title="📂 Буманың ішін тізімдеу (Python)"
  language="python"
  code={`import os

def list_items(directory):
    items = os.listdir(directory)

    for item in items:
        item_path = os.path.join(directory, item)
        if os.path.isdir(item_path):
            print(f"{item}: бума")
        else:
            print(f"{item}: файл")

list_items('../')`}
/>

### Жылдам салыстыру

| Мүмкіндік | JavaScript | Python |
|-----------|------------|--------|
| Файл не бума бар-жоғын тексеру | `fs.existsSync(path)` | `os.path.exists(path)` |
| Бума жасау | `fs.mkdirSync(path)` | `os.mkdir(path)` |
| Буманы оқу (тізімін алу) | `fs.readdirSync(path)` | `os.listdir(path)` |

### Ресурстар

- https://docs.python.org/3/library/os.html
