---
id: math-module
slug: /math-module
title: Math модулі
sidebar_label: Math модулі
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';


Python тіліндегі `math` модулі — әртүрлі математикалық функциялар мен тұрақтыларды ұсынатын кітапхана.

---

### Тапсырма

Берілген радиус бойынша шеңбердің ауданын есептейтін бағдарлама жазыңыз.

---

#### JavaScript іске асыруы
<CodePythonBlock
  title="📄 JavaScript"
  language="javascript"
  code={`const radius = 5;
const area = Math.PI * Math.pow(radius, 2);
console.log(area);`}
/>


---

#### Python іске асыруы
<CodePythonBlock
  title="📄 Python"
  language="python"
  code={`import math

radius = 5
area = math.pi * math.pow(radius, 2)
print(area)`}
/>


---

### Жылдам салыстыру

| Мүмкіндік                          | JavaScript                 | Python                      |
|-----------------------------------|-----------------------------|------------------------------|
| Модуль атауы                      | `Math`                     | `math`                       |
| Модульді импорттау               | Қажет емес (бүкіл жерде бар) | `import math`                |
| Модуль тұрақтысы (π)              | `Math.PI`                  | `math.pi`                    |
| Модуль тұрақтысы (e)              | `Math.E`                   | `math.e`                     |
| Модуль тұрақтысы (tau)            | —                          | `math.tau`                   |
| Абсолюттік мән                   | `Math.abs(x)`              | `abs(x)`                     |
| Жақын бүтін санға дөңгелектеу     | `Math.round(x)`            | `round(x)`                   |
| Жоғарыға дөңгелектеу              | `Math.ceil(x)`             | `math.ceil(x)`              |
| Төменге дөңгелектеу              | `Math.floor(x)`            | `math.floor(x)`             |
| Дәреже                           | `Math.pow(x, y)`           | `math.pow(x, y)`            |
| Квадрат түбір                    | `Math.sqrt(x)`             | `math.sqrt(x)`              |
| Тригонометриялық функциялар       | `Math.sin(x)`<br />`Math.cos(x)`<br />`Math.tan(x)` және т.б. | `math.sin(x)`<br />`math.cos(x)`<br />`math.tan(x)` және т.б. |
| Градус → радиан                  | —                          | `math.radians(x)`           |
| Радиан → градус                  | —                          | `math.degrees(x)`           |

---

### Ресурстар

- [Python ресми құжаттамасы — math модулі](https://docs.python.org/3/library/math.html)
