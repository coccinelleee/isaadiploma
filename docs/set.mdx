---
id: set
slug: /set
title: Set (Жиын)
sidebar_label: Set
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';

**Set (жиын)** — бірегей (уникалды) элементтерден тұратын ретсіз коллекция. Бұл JavaScript тіліндегі `Set` құрылымына ұқсас. Жиі **қайталауларды жою** үшін қолданылады.

---

### 🧪 Тапсырма

Мәтінді бос орын бойынша сөздерге бөліңіз, бірдей сөздерді алып тастаңыз және нәтижені сұрыптаңыз.

---

#### ✅ Python іске асыруы

<CodePythonBlock
  title="🚀 JavaScript: Қайталаусыз сұрыпталған сөздер"
  language="javascript"
  code={`const text = "apple banana cherry apple banana";
const words = text.split(" ");
const uniqueWords = [...new Set(words)];
uniqueWords.sort();
console.log(uniqueWords);`}
/>

---

#### ✅ Келесі:

<CodePythonBlock
  title="🐍 Python: Қайталаусыз сұрыпталған сөздер"
  language="python"
  code={`text = "apple banana cherry apple banana"
words = text.split(" ")
unique_words = set(words)
sorted_words = sorted(unique_words)
print(sorted_words)`}
/>

---

### 🧠 Кодты түсіндіру

- Python тілінде жиын (set) құру үшін `set()` функциясы қолданылады.
- JavaScript тілінде `new Set()` синтаксисі арқылы жиын құрылады.
- Жиын ішіндегі элементтер ретсіз болады, бірақ Python-да `sorted()` көмегімен оларды сұрыптауға болады.

---

### ⚖️ Жылдам салыстыру

| Мүмкіндік                           | JavaScript                     | Python                             |
|------------------------------------|--------------------------------|------------------------------------|
| Жиын құру                          | `let mySet = new Set()`        | `my_set = set()`                   |
| Элемент қосу                       | `mySet.add(el)`                | `my_set.add(el)`                   |
| Өлшемін тексеру                    | `mySet.size`                   | `len(my_set)`                      |
| Бос па, тексеру                    | `mySet.size === 0`             | `len(my_set) == 0`                 |
| Элементті жою                      | `mySet.delete(el)`             | `my_set.remove(el)`                |
| Жиынды тазалау                     | `mySet.clear()`                | `my_set.clear()`                   |
| Элемент бар-жоғын тексеру          | `mySet.has(el)`                | `el in my_set`                     |
| Жиынды тізімге/массивке түрлендіру| `[...mySet]`                   | `list(my_set)`                     |
| Жиындардың бірігуі (union)         | —                              | `my_set.union(other_set)`          |
| Қиылысу (intersection)             | —                              | `my_set.intersection(other_set)`   |

---

### 📚 Ресурстар

- [Python құжаттамасы: set түрлері](https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset)
