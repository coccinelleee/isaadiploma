---
id: dictionary
slug: /dictionary
title: Сөздік (Dictionary)
sidebar_label: Сөздік
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';


Сөздіктер (dictionary) деректерді кілттер арқылы сақтау және алу үшін жиі қолданылады. Python тіліндегі сөздіктер JavaScript тіліндегі объектілерге ұқсайды.

### Тапсырма

Оқушылардың жеке ақпаратын сақтау және алу үшін сөздік қолданатын бағдарлама құрыңыз. Жеке ақпаратқа аты, жасы және қаласы кіреді.

#### JavaScript іске асыруы
<CodePythonBlock
  title="📄 JavaScript"
  language="javascript"
  code={`let personalInfo = {};
 
personalInfo.name = 'luckrnx09';
personalInfo.age = 18;
personalInfo.city = 'Chengdu';

console.log(personalInfo);`}
/>


#### Python іске асыруы
<CodePythonBlock
  title="📄 Python"
  language="python"
  code={`personal_info = {}

personal_info["name"] = 'luckrnx09'
personal_info["age"] = 18
personal_info["city"] = 'Chengdu'

print(personal_info)`}
/>


### Кодты түсіндіру

- Python және JavaScript тілдерінде де бос сөздікті немесе объектіні жасау үшін `{}` қолданылады.
- Python тілінде мәндерді тек шаршы жақша арқылы алуға болады (`my_dict[key]`), ал JavaScript тілінде қосымша нүктелік жазба (`myObj.key`) да қолданылады.

### Жылдам салыстыру

| Мүмкіндік | JavaScript | Python |
|-----------|-------------|--------|
| Сөздік анықтау | `let obj = {};` <br /> `let obj = {key1: value1, key2: value2};` | `dict1 = {}` <br /> `my_dict = {"key1": value1, "key2": value2}` |
| Мәнге қол жеткізу | `obj[key];` | `my_dict[key]` |
| Мәнге немесе әдепкі мәнге қол жеткізу | `obj[key] ?? defaultVal;` | `my_dict.get(key, default_value)` |
| Мәнді жаңарту | `obj[key] = newValue;` | `my_dict[key] = newValue` |
| Қосу және жаңарту | `obj={...obj,...anotherObj}` | `my_dict.update(another_dict)` |
| Кілт-мән жұбын жою | `delete obj[key];` | `del my_dict[key]` |
| Кілт бар-жоғын тексеру | `key in obj;` | `key in my_dict` |
| Барлық кілттерді алу | `Object.keys(obj);` | `my_dict.keys()` |
| Барлық мәндерді алу | `Object.values(obj);` | `my_dict.values()` |
| Кілт-мән жұптарының санын алу | `Object.keys(obj).length;` | `len(my_dict)` |

:::danger
Python тілінде `{}` синтаксисі арқылы сөздік жасағанда, кілттер міндетті түрде тырнақшада жазылуы керек. Сонымен қатар, `dict()` функциясы арқылы да сөздік жасауға болады, онда кілттерді тырнақшасыз жазуға болады:
:::

<CodePythonBlock
  title="🔧 dict() арқылы сөздік жасау"
  language="python"
  code={`my_dict = dict(a=1, b=2)
print(my_dict)`}
/>


### Ресурстар

- [Python құжаттамасы: Сөздіктер](https://docs.python.org/3/tutorial/datastructures.html#dictionaries)
