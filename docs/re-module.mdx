---
id: re-module
slug: /re-module
title: Re модулі
sidebar_label: Re модулі
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';


**Регулярлы өрнектер (Regular Expressions)** — мәтінмен жұмыс істеуге, іздеуге, сәйкестікті тексеруге және өзгертулер енгізуге арналған қуатты құрал. Python тілінде `re` модулі осы функционалдылықты қамтамасыз етеді.

---

### Тапсырма

Электрондық пошталар тізімін өңдейтін бағдарлама жазыңыз. Бағдарлама әр поштадан **пайдаланушы атын** және **доменін** регулярлы өрнек арқылы бөліп алуы керек.

---

#### Python іске асыруы

<CodePythonBlock
  title="📧 Email ішінен мәліметтерді алу (Python)"
  language="python"
  code={`import re

email_addresses = [
  "john.doe@example1.com",
  "jane.smith@example2.com",
  "foo@bar.com",
]

regex = re.compile(r"([^@]+)@(.+)")

for email in email_addresses:
    match = regex.match(email)
    if match:
        username, domain = match.groups()
        print(f"Username: {username}, Domain: {domain}")`}
/>


---

### Python іске асыруы

<CodePythonBlock
  title="📧 Python — Email бөліп алу"
  language="python"
  code={`import re

email_addresses = [
  "john.doe@example1.com",
  "jane.smith@example2.com",
  "foo@bar.com",
]

regex = re.compile(r"([^@]+)@(.+)")

for email in email_addresses:
  match = regex.match(email)
  if match:
    username, domain = match.groups()
    print(f"Username: {username}, Domain: {domain}")`}
/>

---

:::tip
Python-да жолдың алдында `r` әрпін қолдану — **“raw string”** (өңделмеген жол) дегенді білдіреді. Бұл жағдайда `\` символдары арнайы мағынаға ие болмайды және тура сол күйінде өңделеді.
:::

---

### Кодты түсіндіру

- JavaScript тілінде регулярлы өрнек `/.../` немесе `new RegExp(...)` арқылы құрылады.
- Python тілінде `re.compile(r'өрнек')` синтаксисі пайдаланылады.

---

### Жылдам салыстыру

| Мүмкіндік                             | JavaScript                              | Python                                 |
|--------------------------------------|------------------------------------------|----------------------------------------|
| Регулярлы өрнек құру                 | `/pattern/` немесе `new RegExp(pattern)` | `re.compile(r'pattern')`               |
| Сәйкестік тексеру (`True/False`)     | `myReg.test(myStr)`                     | `my_regex.match(my_str)`               |
| Бірінші сәйкестікті табу             | `myReg.exec(myStr)`                     | `my_regex.search(my_str)`              |
| Барлық сәйкестіктерді табу           | `myStr.match(myReg)`                    | `my_regex.findall(my_str)`             |
| Мәтінді ауыстыру                     | `myStr.replace(myReg, replacement)`     | `my_regex.sub(replacement, my_str)`    |
| Мәтінді бөлшектеу (split)            | `myStr.split(myReg)`                    | `my_regex.split(my_str)`               |

---
:::tip
`re.compile()` арқылы өрнек объектісі жасау міндетті емес. Python-да `re.search()` немесе `re.split()` сияқты функцияларды тікелей қолдануға болады:
:::

<CodePythonBlock
  title="🔎 Регулярлы өрнек — re.search және re.split"
  language="python"
  code={`import re

re.search(r'pattern', my_str)
re.split(r'pattern', my_str)`}
/>

---

### Ресурстар

- [Python — re модулі құжаттамасы](https://docs.python.org/3/library/re.html)
