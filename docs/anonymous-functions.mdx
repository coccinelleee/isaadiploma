---
id: anonymous-functions
slug: /anonymous-functions
title: Анонимді функциялар
sidebar_label: Анонимді функциялар
---

import ToggleCodeBlock from '@site/src/components/ToggleCodeBlock';

Егер сізге шағын код үзіндісін бірден орындау қажет болып, жеке функция анықтаудың қажеті болмаса, онда анонимді функцияларды қолдануға болады. Python тілінде анонимді функциялар "lambda өрнектері" немесе "lambda функциялары" деп аталады.

### Тапсырма

Анонимді функцияларды қолданып, сандар тізіміндегі әрбір элементті оның квадратына түрлендіріп, жаңа тізімді басып шығарыңыз.

#### JavaScript іске асыруы

<ToggleCodeBlock
  title="📄 JavaScript"
  language="javascript"
  code={`const numbers = [1, 2, 3, 4, 5];

const squaredNumbers = numbers.map(number => number ** 2);
console.log(squaredNumbers);`}
/>

#### Python іске асыруы

<ToggleCodeBlock
  title="📄 Python"
  language="python"
  code={`numbers = [1, 2, 3, 4, 5]

squared_numbers = list(map(lambda number: number ** 2, numbers))
print(squared_numbers)`}
/>

### Кодты түсіндіру
- Python тілінде анонимді функция жасау үшін `lambda` кілт сөзі қолданылады, ал JavaScript тілінде `function` немесе қысқаша жазылуы `=>` пайдаланылады.
- Python-дағы `lambda` функцияларында толық функция денесі болмайды, тек бір өрнек жазылады және оның нәтижесі автоматты түрде қайтарылады. Ал JavaScript анонимді функцияларында толық функция денесі және `return` операторы болуы мүмкін.

### Жылдам салыстыру

| Ерекшелік    | JavaScript                       | Python       |
|--------------|----------------------------------|--------------|
| Анонимді функцияны жасау | `(params) => expression`         | `lambda params: expression` |

:::tip
Егер Python тіліндегі `lambda` функциясында бірнеше параметр болса, оларды үтір `,` арқылы бөліңіз.
:::

### Ресурстар

- https://docs.python.org/3/tutorial/controlflow.html#lambda-expressions
