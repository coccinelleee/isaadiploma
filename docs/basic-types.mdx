---
id: basic-types
slug: /basic-types
title: Негізгі деректер типтері
sidebar_label: Негізгі деректер типтері
---

import ToggleCodeBlock from '@site/src/components/ToggleCodeBlock';

Python тіліндегі негізгі деректер типтеріне мыналар жатады:

- **Integer (бүтін сан)**: Бүтін сандар, ондық нүктесіз.
- **Float (нақты сан)**: Ондық нүктесі бар сандар.
- **String (жол)**: Бір немесе қос тырнақшаға алынған символдар тізбегі.
- **Boolean (логикалық)**: `True` немесе `False` мәндері.
- **None**: Бос мәнді білдіреді.

### Тапсырма

Адамның жалақы парағын сипаттайтын 5 айнымалыны анықтаңыз: аты, айы, жалақысы, мінсіз қатысымы бар ма, және жобалық тобы.

#### JavaScript іске асыруы

<ToggleCodeBlock
  title="📄 JavaScript"
  language="javascript"
  code={`let name = 'Bob' // жол (string)
let month = 12 // сан (number)
let salary = 10000.12 // сан (number)
let isPerfectAttendance = true // логикалық (boolean)
let team = null // бос мән (null)
console.log(typeof name) // Айнымалының типін анықтау`}
/>

#### Python іске асыруы

<ToggleCodeBlock
  title="📄 Python"
  language="python"
  code={`name = 'Bob' # str (жол)
month = 12 # int (бүтін сан)
salary = 10000.12 # float (нақты сан)
is_perfect_attendance = True # bool (логикалық)
team = None # None (бос мән)
print(type(name)) # Айнымалының типін анықтау`}
/>

### Кодты түсіндіру
- Python тілінде `int` — бүтін сандар үшін, `float` — ондық сандар үшін, ал JavaScript екі түрге де ортақ `number` типін қолданады.
- Python `None` бос мән ретінде қолданады, ал JavaScript — `null`.
- Python тілінде `undefined` сияқты ұғым жоқ.
- Python айнымалының типін білу үшін `type()` функциясын, ал JavaScript — `typeof` операторын қолданады.

### Жылдам салыстыру

| Тип       | JavaScript                              | Python                     |
|-----------|------------------------------------------|----------------------------|
| Integer   | `let myInt = 10;`<br />`let myInt = Number(10);` | `my_int = 10`              |
| Float     | `let myFloat = 3.14;`<br />`let myFloat = Number(3.14);` | `my_float = 3.14`     |
| String    | `let myStr = "Hello World";`             | `my_str = "Hello World"`   |
| Boolean   | `let myBool = true;`<br />`let myBool = false;` | `my_bool = True`<br />`bool_val = False` |
| None/null | `let myNull = null;`                     | `my_none = None`           |

Жол (string) — бағдарламалау тілдеріндегі ең жиі қолданылатын типтердің бірі. Олармен жұмыс істеуді меңгеру программалауды едәуір жеңілдетеді. Төменде JavaScript және Python тілдерінде жолдармен жұмыс істеудің ең жиі қолданылатын әдістері берілген:

| Әдіс                          | JavaScript                               | Python                             |
|-------------------------------|------------------------------------------|------------------------------------|
| Ұзындығын алу                 | `myStr.length`                           | `len(my_str)`                      |
| Жолдарды біріктіру            | `myStr1 + myStr2`                        | `my_str1 + my_str2`                |
| Жолды қиып алу (slice)        | `myStr.slice(start, end)`               | `my_str[start:end]`                |
| Ішінен табу                   | `myStr.indexOf(substring)`              | `my_str.index(substring)`          |
| Ауыстыру                      | `myStr.replace(old, new)`               | `my_str.replace(old, new)`         |
| Бөлу                          | `myStr.split(separator)`                | `my_str.split(separator)`          |
| Үлкен/кіші әріпке түрлендіру | `myStr.toUpperCase()`<br />`myStr.toLowerCase()` | `my_str.upper()`<br />`my_str.lower()` |
| Жолды кері аудару             | `myStr.split('').reverse().join('')`    | `my_str[::-1]`                     |
| Қосалқы жол жиілігі           | `myStr.split(substring).length - 1`     | `my_str.count(substring)`          |
| Басталатын әріпті тексеру     | `myStr.startsWith(prefix)`              | `my_str.startswith(prefix)`        |
| Аяқталатын әріпті тексеру     | `myStr.endsWith(suffix)`                | `my_str.endswith(suffix)`          |
| Артық бос орындарды жою       | `myStr.trim()`                          | `my_str.strip()`                   |
| Ортасына туралау              | –                                       | `my_str.center(width)`             |
| Алғашқы әріпті бас әріпке айналдыру | `myStr.replace(myStr[0], myStr[0].toUpperCase())` | `my_str.capitalize()`       |
| Әр сөздің бірінші әрпін бас әріпке айналдыру | `myStr.replace(/(^\w{1})\|(\s+\w{1})/g, s => s.toUpperCase())` | `my_str.title()`         |

### Ресурстар
- https://docs.python.org/3/library/stdtypes.html
