---
id: custom-exception-classes
slug: /custom-exception-classes
title: Арнайы ерекшеліктер (Custom Exceptions)
sidebar_label: Арнайы ерекшеліктер
---

import ToggleCodeBlock from '@site/src/components/ToggleCodeBlock';

Python тілінде қателік жағдайларын нақтырақ өңдеу үшін өзіңіздің арнайы ерекшеліктеріңізді (custom exceptions) жасай аласыз. Бұл бағдарламадағы қателіктер туралы түсінікті және мағыналы хабарламалар жасауға мүмкіндік береді.

### Тапсырма

Әртүрлі фигуралардың ауданын есептейтін бағдарлама жазыңыз. Әр түрлі фигураларға байланысты есептеу қателіктерін өңдеу үшін арнайы ерекшеліктерді (custom exceptions) қолданыңыз.

#### JavaScript іске асыруы

<ToggleCodeBlock
  title="📄 JavaScript"
  language="javascript"
  code={`class InvalidShapeException extends Error {
  constructor(message) {
    super(message);
    this.name = "InvalidShapeException";
  }
}

class NegativeDimensionException extends Error {
  constructor(message) {
    super(message);
    this.name = "NegativeDimensionException";
  }
}

function calculateRectangleArea(length, width) {
  if (typeof length !== "number" || typeof width !== "number") {
    throw new InvalidShapeException("Өлшемдер дұрыс емес. Ұзындық пен ен тек сандар болуы керек.");
  }
  if (length <= 0 || width <= 0) {
    throw new NegativeDimensionException("Өлшемдер теріс немесе нөлге тең болмауы керек.");
  }
  return length * width;
}

console.log(calculateRectangleArea(5, 4)); // Нәтиже: 20
console.log(calculateRectangleArea("5", 4)); // InvalidShapeException қателігін шығарады
console.log(calculateRectangleArea(-5, 4)); // NegativeDimensionException қателігін шығарады`}
/>

#### Python іске асыруы

<ToggleCodeBlock
  title="📄 Python"
  language="python"
  code={`# Арнайы ерекшеліктерді анықтау
class InvalidShapeException(Exception):
    def __init__(self, message):
        super().__init__(message)

class NegativeDimensionException(Exception):
    def __init__(self, message):
        super().__init__(message)

def calculate_rectangle_area(length, width):
    if not isinstance(length, (int, float)) or not isinstance(width, (int, float)):
        raise InvalidShapeException("Өлшемдер дұрыс емес. Ұзындық пен ен тек сандар болуы керек.")
    if length <= 0 or width <= 0:
        raise NegativeDimensionException("Өлшемдер теріс немесе нөлге тең болмауы керек.")
    return length * width

print(calculate_rectangle_area(5, 4))  # Нәтиже: 20
print(calculate_rectangle_area("5", 4))  # InvalidShapeException қателігін шығарады
print(calculate_rectangle_area(-5, 4))  # NegativeDimensionException қателігін шығарады`}
/>

### Кодты түсіндіру

- Python тілінде барлық ерекшеліктер `Exception` класынан туындайды, ал JavaScript тілінде `Error` класынан. Екі тілде де арнайы ерекшеліктерді жасау үшін `class` қолданылады.
- Python `isinstance()` функциясы арқылы айнымалының типін тексереді, ал JavaScript `typeof` немесе `instanceof` қолданады.

### Жалпы салыстыру

| Ерекшелік                | JavaScript                          | Python                                |
|--------------------------|-------------------------------------|---------------------------------------|
| Арнайы ерекшелік жасау    | `class CustomException extends Error {}` | `class CustomException(Exception):`   |
| Ерекшелікті лақтыру      | `throw new CustomException(message)`    | `raise CustomException(message)`      |

### Ресурстар

- [Python: Қателіктер және ерекшеліктер құжаттамасы](https://docs.python.org/3/tutorial/errors.html)
