---
id: pip
slug: /pip
title: Pip
sidebar_label: Pip
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';

Python тілінде `pip` — үшінші тарап кітапханаларын орнатуға арналған әдепкі пакет менеджері. Ол JavaScript жобаларындағы `npm` сияқты жұмыс істейді.

---

### Пакеттерді орнату

Python ортасын орнатқанда, `pip` құралы операциялық жүйеге автоматты түрде орнатылады. Қажетті кітапханаларды орнату үшін келесі команданы пайдалануға болады:

<CodePythonBlock
  title="📦 Бірнеше пакетті орнату"
  language="bash"
  code={`pip install package_name1 package_name2`}
/>

Бұл JavaScript жобаларындағы `npm install package1 package2` командасына ұқсас.

Нақты бір нұсқадағы кітапхананы орнату үшін `==` таңбасын пайдаланыңыз:

<CodePythonBlock
  title="📌 Белгілі бір нұсқадағы кітапхананы орнату"
  language="bash"
  code={`pip install requests==2.20.0`}
/>

Бұл `npm install axios@0.21.1` командасымен бірдей қызмет атқарады.

:::tip Бағдарламалық қамтамасыз ету репозиторийі
Python тілінің ресми бағдарламалар репозиториясы: [https://pypi.org](https://pypi.org)
:::

---

### Пакеттерді жою

<CodePythonBlock
  title="🗑 Пакетті жою"
  language="bash"
  code={`pip uninstall package_name`}
/>

Бұл `npm uninstall package1 package2` командасына ұқсас.

:::tip
Python — тәуелділіктерді орталықтандырылған түрде басқаруды қолданады. Яғни, орнатылған кітапханалар **жоба қалтасына емес**, Python интерпретаторы орналасқан жүйелік қалтаға жазылады.

`pip install` арқылы орнатылған кітапханалар — техникалық тұрғыдан **глобалды** болып есептеледі. Бұл JavaScript жобаларындағы `node_modules` ішіне сақталатын `npm` кітапханаларынан ерекшеленеді.
:::

---

### Жоба тәуелділіктерінің тізімін жасау

JavaScript тілінде `package.json` ішінде `dependencies` және `devDependencies` арқылы тәуелділіктер сақталады. Python-да бұл автоматты түрде жасалмайды.

Python жобасында тәуелділіктерді сақтау үшін келесі команданы орындаңыз:

<CodePythonBlock
  title="📝 Тәуелділіктерді requirements.txt файлына сақтау"
  language="bash"
  code={`pip freeze > requirements.txt`}
/>

Содан кейін басқа адамдар осы файл арқылы барлық кітапханаларды оңай орната алады:

<CodePythonBlock
  title="📥 requirements.txt арқылы барлық тәуелділіктерді орнату"
  language="bash"
  code={`pip install -r requirements.txt`}
/>

:::tip requirements.txt ішінде тым көп кітапхана бар ма?

Кейде `requirements.txt` ішінде жоба пайдаланбайтын көптеген кітапханалар болады. Бұл барлық кітапханалар **жүйедегі ағымдағы Python ортасына** қатысты болғандықтан орын алады.

Егер тек жобаға қажетті кітапханаларды ғана тізгіміз келсе, **виртуалды орта (venv)** қолдану қажет.
:::

---

### Ресурстар

- https://pip.pypa.io/en/stable/
