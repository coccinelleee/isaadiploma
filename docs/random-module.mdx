---
id: random-module
slug: /random-module
title: Random модулі
sidebar_label: Random модулі
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';

Python тіліндегі `random` модулі — кездейсоқ сандарды генерациялауға арналған функцияларды ұсынады.

---

### Тапсырма

Кездейсоқ **32 таңбалы** құпиясөз жасаңыз. Ол кіші әріптер, бас әріптер және сандардан тұруы керек.

---

### JavaScript іске асыруы

<CodePythonBlock
  title="🔐 JavaScript арқылы құпиясөз генерациясы"
  language="javascript"
  code={`function generatePassword() {
  var length = 32;
  var charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  var password = "";
  
  for (var i = 0; i < length; i++) {
    var randomIndex = Math.floor(Math.random() * charset.length);
    password += charset.charAt(randomIndex);
  }
  
  return password;
}

var password = generatePassword();
console.log(password);`}
/>

---

### Python іске асыруы

<CodePythonBlock
  title="🔐 Python арқылы құпиясөз генерациясы"
  language="python"
  code={`import random
import string

def generate_password():
    length = 32
    charset = string.ascii_letters + string.digits
    password = ''.join(random.choice(charset) for _ in range(length))
    return password

password = generate_password()
print(password)`}
/>

---

### Кодты түсіндіру

- Python тіліндегі `string` модулі — `ascii_letters` (барлық кіші және бас әріптер) және `digits` (0–9 сандары) жиындарын ұсынады.
- Python-да `random.randint()` — берілген диапазонда кездейсоқ бүтін сан қайтарады. JavaScript-те мұны `Math.random()` және `Math.floor()` көмегімен өзіміз жазуымыз керек.

---

### Жылдам салыстыру

| Мүмкіндік                              | JavaScript                                                  | Python                                 |
|----------------------------------------|-------------------------------------------------------------|----------------------------------------|
| 0 мен 1 арасында кездейсоқ сан          | `Math.random()`                                             | `random.random()`                      |
| Диапазонда кездейсоқ бүтін сан         | `Math.floor(Math.random() * (max - min + 1)) + min`         | `random.randint(min_val, max_val)`     |
| Тізімнен кездейсоқ элемент таңдау      | —                                                           | `random.choice(sequence)`              |
| Тізімді араластыру (shuffle)           | —                                                           | `random.shuffle(sequence)`             |
| Диапазонда кездейсоқ қалқымалы сан     | —                                                           | `random.uniform(min_val, max_val)`     |
| Кездейсоқтықты алдын ала белгілеу (seed) | —                                                         | `random.seed(seed_val)`                |

---

### Ресурстар

- [Python — Random модулі құжаттамасы](https://docs.python.org/3/library/random.html)
