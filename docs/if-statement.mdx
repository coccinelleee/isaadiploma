---
id: if-statement
slug: /if-statement
title: If операторы
sidebar_label: If операторы
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';


`if` операторы — белгілі бір шарттың орындалуына байланысты кодтың орындалуын басқаратын басқару құрылымы. Python мен JavaScript тілдерінде бұл оператордың функциясы ұқсас болғанымен, синтаксисінде елеулі айырмашылықтар бар.

---

### Тапсырма

Берілген санның **оң**, **теріс**, немесе **нөл** екенін анықтайтын бағдарлама жазыңыз.

#### JavaScript іске асыруы
<CodePythonBlock
  title="📄 JavaScript"
  language="javascript"
  code={`const number = 100;

if (number > 0) {
    console.log("Оң сан");
} else if (number < 0) {
    console.log("Теріс сан");
} else {
    console.log("Нөл");
}`}
/>


#### Python іске асыруы
<CodePythonBlock
  title="📄 Python"
  language="python"
  code={`number = 100

if number > 0:
    print("Оң сан")
elif number < 0:
    print("Теріс сан")
else:
    print("Нөл")`}
/>


---

### Кодты түсіндіру

- Python тілінде `if` шартында жақша қажет емес, ал JavaScript тілінде шарт міндетті түрде жақша ішінде жазылады.
- Python тілінде `:` таңбасы шарттан кейін қойылады, және блок **шегініс** (әдетте 4 бос орын) арқылы белгіленеді. JavaScript тілінде `{}` блок үшін қолданылады.
- Python `elif` кілт сөзін қолданады, ал JavaScript — `else if`.

---

:::danger Код шегінісі (индентация)
Python тілінде шегініс — міндетті. Әр блок 4 бос орынмен немесе табуляциямен ерекшеленеді. Егер шегініс дұрыс болмаса, синтаксистік қате пайда болады.

Кейде блок ішінде нақты әрекет болмаса, `pass` операторын қолдануға болады:
```python
if some_condition:
    pass  # Ештеңе істеме
else:
    ...
```
:::

---

### Жылдам салыстыру

| Мүмкіндік                  | JavaScript                               | Python                                 |
|----------------------------|-------------------------------------------|----------------------------------------|
| `if` операторы             | `if (шарт) { код }`                       | `if шарт:`                             |
| `else` операторы           | `else { код }`                            | `else:`                                |
| `else if` / `elif`         | `else if (шарт) { код }`                  | `elif шарт:`                           |
| Ішкі if (қабаттасқан)      | Иә                                       | Иә                                     |
| Логикалық операторлар      | `&&`, `||`, `!`                           | `and`, `or`, `not`                     |
| `in` және `includes`       | `key in myObj`, `arr.includes(el)`        | `key in my_dict`, `el in my_list`      |
| Теңдік                     | `===`                                     | `==`                                   |
| Тең емес                   | `!==`                                     | `!=`                                   |
| Үлкен                      | `>`                                       | `>`                                    |
| Кіші                       | `<`                                       | `<`                                    |
| Үлкен немесе тең           | `>=`                                      | `>=`                                   |
| Кіші немесе тең            | `<=`                                      | `<=`                                   |
| Тернарлық оператор         | `let result = cond ? 'True' : 'False'`   | `result = 'True' if cond else 'False'`|

---

:::tip None мәнін тексеру (Null checking)
Python тілінде мәннің `None` екенін тексерудің төрт тәсілі бар:
- `is None`
- `is not None`
- `== None`
- `!= None`
:::

:::tip Ескерту
Python тілінде `switch-case` синтаксисі жоқ.
:::

---

### Ресурстар

- [Python if операторы туралы құжаттама](https://docs.python.org/3/reference/compound_stmts.html#the-if-statement)
- [Python салыстыру операторлары](https://docs.python.org/3/library/stdtypes.html#comparisons)
