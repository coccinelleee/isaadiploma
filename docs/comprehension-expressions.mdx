---
id: comprehension-expressions
slug: /comprehension-expressions
title: Тізім мен Сөздік генераторлары
sidebar_label: Тізім мен Сөздік генераторлары
---

import ToggleCodeBlock from '@site/src/components/ToggleCodeBlock';


Python тілінде тізім генераторлары (list comprehensions) және сөздік генераторлары (dictionary comprehensions) арқылы тізімдер мен сөздіктерді қысқа әрі ықшам түрде құруға болады.

### Тапсырма
1. 1-ден 10-ға дейінгі сандардың квадраттарынан тізім жасап, содан кейін тақ сандарды алып тастаңыз.
2. 1-ден 10-ға дейінгі сандардың квадраттарынан тұратын сөздік жасаңыз (сан — кілт, квадраты — мән), кейін мәні 5 немесе одан үлкен жұптарды алып тастаңыз.

#### JavaScript іске асыруы

<ToggleCodeBlock
  title="📄 JavaScript"
  language="javascript"
  code={`// Тапсырма 1:
const numbers = Array.from({ length: 10 }, (_, i) => (i + 1) ** 2);
const filteredNumbers = numbers.filter(num => num % 2 === 0);
console.log(filteredNumbers);

// Тапсырма 2:
const dictionary = {};
for (let i = 1; i <= 10; i++) {
  dictionary[i] = i ** 2;
}
const filteredDictionary = Object.fromEntries(
  Object.entries(dictionary).filter(([key, value]) => value < 5)
);
console.log(filteredDictionary);`}
/>

#### Python іске асыруы

<ToggleCodeBlock
  title="📄 Python"
  language="python"
  code={`# Тапсырма 1:
squared_numbers = [x**2 for x in range(1, 11)]
filtered_numbers = [num for num in squared_numbers if num % 2 == 0]
print(filtered_numbers)

# Тапсырма 2:
squared_dict = {x: x**2 for x in range(1, 11)}
filtered_dict = {key: value for key, value in squared_dict.items() if value < 5}
print(filtered_dict)`}
/>


:::tip
Тізім және сөздік генераторлары — тек Python тіліне тән синтаксис. JavaScript тілінде олардың толық баламасы жоқ.
:::

### Кодты түсіндіру
- Python тізім генераторлары шаршы жақшаларға алынып жазылады және жаңа тізім қайтарады. Синтаксисі: `[өрнек for элемент in тізім if шарт]`.
- Python сөздік генераторлары фигуралы жақшаларға алынып жазылады және жаңа сөздік қайтарады. Синтаксисі: `{кілт_өрнек: мән_өрнек for элемент in итерируемый_объект if шарт]`.
- Тізім мен сөздік генераторларында күрделі шарттар мен өзгерістер жасауға болады. Бұл оларды өте қуатты құрал етеді.

### Ресурстар

- [Python тізім генераторлары](https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions)
- [Python сөздік генераторлары](https://docs.python.org/3/tutorial/datastructures.html#dictionaries)
