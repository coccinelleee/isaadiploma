---
id: venv-and-conda
slug: /venv-and-conda-and-condaip
title: Виртуалды орта және Conda
sidebar_label: Виртуалды орта және Conda
---

import CodePythonBlock from '@site/src/components/CodePythonBlock';

Python тілінде виртуалды орталар — әрбір жоба үшін жеке Python ортасын жасауға мүмкіндік беретін құрал. Бұл әр жоба үшін тәуелділіктерді оқшаулауға мүмкіндік береді. Әртүрлі жобаларда әртүрлі кітапхана нұсқаларын бір-біріне кедергі келтірмей орнатуға болады. Бұл JavaScript бағдарламашылары жиі қолданатын **nvm** құралына ұқсас.

---

### Виртуалды ортаның жұмыс істеу принципі

Виртуалды ортаның үш маңызды бөлігі бар: ортаны жасау, оны белсендіру және тәуелділіктерді орнату.

Виртуалды орта — бұл арнайы Python орындалатын файлы мен қажетті тәуелділіктерді қамтитын қалта. Бұл ортаны белсендіру кезінде терминалдағы барлық `python` және басқа тиісті командалар сол ортаның ішіндегі файлдарға бағытталады. Осылайша жобалар арасындағы тәуелділіктер толық оқшауланады.

---

### venv арқылы виртуалды орталарды басқару

**venv** — Python-ның әдепкі виртуалды ортаны басқару құралы.

#### Виртуалды ортаны жасау

<CodePythonBlock
  title="🛠️ Виртуалды орта жасау (venv)"
  language="bash"
  code={`python -m venv myvenv`}
/>

#### Виртуалды ортаны белсендіру

<CodePythonBlock
  title="▶️ Виртуалды ортаны іске қосу (venv)"
  language="bash"
  code={`source ./myvenv/bin/activate`}
/>

#### Виртуалды ортаны өшіру (deactivate)

<CodePythonBlock
  title="⛔ Виртуалды ортаны өшіру"
  language="bash"
  code={`deactivate`}
/>

:::tip
Қай Python орындалатынын тексеру үшін кез келген уақытта:

<CodePythonBlock
  language="bash"
  code={`which python`}
  title="🔍 Белсенді Python жолын тексеру"
/>
:::

---

### Conda арқылы виртуалды орталарды басқару

**conda** — виртуалды орталар мен пакеттерді басқаруға арналған құрал. Қолданар алдында оны [ресми құжаттама](https://conda.io/projects/conda/en/latest/user-guide/install/index.html) арқылы орнатып алыңыз.

#### Виртуалды ортаны жасау

<CodePythonBlock
  title="🛠️ Виртуалды орта жасау (conda)"
  language="bash"
  code={`conda create --prefix ./myvenv python=3.8`}
/>

#### Виртуалды ортаны белсендіру

<CodePythonBlock
  title="▶️ Conda ортасын белсендіру"
  language="bash"
  code={`conda activate ./myvenv`}
/>

#### Виртуалды ортаны өшіру

<CodePythonBlock
  title="⛔ Conda ортасын өшіру"
  language="bash"
  code={`conda deactivate`}
/>

:::tip Үшінші тарап кітапханаларын орнату
Виртуалды орта белсендірілгеннен кейін, үшінші тарап кітапханаларын:

<CodePythonBlock
  title="📦 pip арқылы пакет орнату"
  language="bash"
  code={`pip install package_name`}
/>

Бұл кітапханалар `virtual_environment_name/lib/python_version/site-packages` қалтасына орнатылады. Осылайша әр жобаның тәуелділіктері бір-бірінен оқшауланады.
:::

---

### Ресурстар

- [https://docs.python.org/3/library/venv.html](https://docs.python.org/3/library/venv.html)  
- [https://conda.io](https://conda.io)
